function main(){
	/*
	//这里data参数是一个媒介 通过上面函数给予
	Init(function(data){  //最终参数函数UIcallback获得解码后数据
		readerUI(data);   //让readerUI函数使用解码数据
	});
	*/
	
	Init(function(data){
		xuanRan(RootContainer,data)
	})
	
	
	
	
}
main();

